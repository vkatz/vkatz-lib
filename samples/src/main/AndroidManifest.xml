<?xml version="1.0" encoding="utf-8"?>
<manifest package="by.vkatz.samples"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:allowBackup="true"
                 android:icon="@drawable/ic_launcher"
                 android:label="Samples">
        <activity android:name=".MainUI"
                  android:configChanges="screenSize|orientation|keyboard|keyboardHidden"
                  android:label="Samples"
                  android:theme="@android:style/Theme.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".fragment_samples.FragTestActivity"/>
    </application>
</manifest>
