buildscript {
    ext.appKotlinVersion = '1.2.50'
    ext.appKotlinCorutines = '0.22.5'
    ext.appAndroidXVersion = '1.0.0-alpha1'
    ext.appConstraintVersion = '1.1.1'

    ext.appMinSdkVersion = 19
    ext.appTargetSdkVersion = 28
    ext.appCompileSdkVersion = 28
    ext.appBuildToolsVersion = '27.0.3'

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0-alpha18'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$appKotlinVersion"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}